rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;
      allo write: if request.resource.data.createdAt == request.time;
    }
  }
}
